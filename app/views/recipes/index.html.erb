<div class="mb-5">

<h1>Favorites</h1>

 <div class="recipes">
    <% @recipes.each do |recipe| %>
      <div class="recipe d-flex flex-column">
        <img src="<%= recipe.image %>" alt="image">
        <%= recipe.name %>
      </div>
    <% end %>
  </div>
</div>
